import React from 'react';

const MainImage = props => {
  return (
    <div>
      Coming Soon !

      <button 
        className="button"
        onClick={() => {     
        }}
      >Dog</button>

    </div>
  );
};

export default MainImage;